import React from 'react';
import Logo from '../assets/logo.svg';

const Banner = () => {
    return (
        <div>
            <img src={Logo} alt="Logo" />
        </div>
    );
};

export default Banner;